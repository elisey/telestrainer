version: '3.9'
services:
    telestrainer:
        build:
            context: ''
            dockerfile: docker/Dockerfile
        container_name: telestrainer
        volumes:
            - service:/project/
        networks:
            - vpc

volumes:
    service:

networks:
  vpc:
    name: telestrainer-network